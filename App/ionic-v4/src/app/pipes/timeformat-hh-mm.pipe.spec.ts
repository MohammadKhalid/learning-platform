import { TimeformatHhMmPipe } from './timeformat-hh-mm.pipe';

describe('TimeformatHhMmPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeformatHhMmPipe();
    expect(pipe).toBeTruthy();
  });
});
